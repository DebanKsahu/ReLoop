services:
  - type: web
    name: ReLoop
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: |
      apt-get update
      apt-get install -y libzbar0
      uv sync
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
